# Public Render API origin used by production Azure Static Web Apps builds.
# Required for Azure SWA (relative /api will not reach Render without this).
# Local Vite can omit this and use the /api proxy in vite.config.js.
VITE_API_ORIGIN=https://library-database-baclend-api.onrender.com
